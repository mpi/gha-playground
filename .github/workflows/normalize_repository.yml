name: Normalize NPM repository
on:
  pull_request:
    types: [ opened, reopened ]
    paths:
      - '**package-lock.json'

permissions:
  pull-requests: write
  contents: write

jobs:
  normalize_repo:
    if: startsWith(github.head_ref, 'dependabot')
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
        with:
          ref: ${{ github.head_ref }}
          github-token: ${{ secrets.GITHUB_TOKEN }}
      - name: Fix repository URL
        run: date +%s > repository.txt
      - name: Commit changes
        run: |
          git config --global user.name 'github-actions[bot]'
          git config --global user.email 'github-actions[bot]@users.noreply.github.com'
          git add repository.txt
          git commit -am "Adjust npm repository url"
          git push

#          git push
#          git config --global user.name 'dependabot[bot]'
#          git config --global user.email 'dependabot[bot]@users.noreply.github.com'
#          git add repository.txt
#          git commit --amend --no-edit || echo "No changes to commit."
#          git push --force-with-lease

#git config --global user.name 'dependabot[bot]'
#git config --global user.email 'dependabot[bot]@users.noreply.github.com'
#git commit -am "Adjust repository" || echo "No changes to commit."
#git push
